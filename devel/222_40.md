# 222_40 在插入->表格 中新增三线表
## 如何测试
- 插入->表格->三线表
- 测试添加行列，是否会自动匹配三条线到首行和末行

## 2026/02/05 修复最小行列数
### What
- 之前写了 `<twith|table-min-cols|2>` 是指最小两列
- 应该写为 `<twith|table-min-rows|2>`，指最小两行

## 2026/02/04 新增三线表
### What
- 在`std-markup.ts`里新写三线表的宏
- 在`table-menu.scm`里添加插入三线表的按钮
