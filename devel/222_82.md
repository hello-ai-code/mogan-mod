# [222_82] 移除查看菜单中的标签页工具栏选项

## 相关文档
- [x_y.md](x_y.md) - 开发文档模板

## 任务相关的代码文件
- `TeXmacs/progs/texmacs/menus/view-menu.scm`
- `TeXmacs/progs/texmacs/texmacs/tm-view.scm`
- `TeXmacs/progs/texmacs/menus/preferences-widgets.scm`
- `src/Texmacs/Data/new_window.cpp`
- `src/Texmacs/Window/tm_window.cpp`
- `src/Graphics/Gui/message.hpp`
- `src/Plugins/Qt/qt_tm_widget.cpp`

## 如何测试
1. 启动 Mogan
2. 点击 `查看` 菜单，确认没有 `标签页工具栏` 选项
3. 确认标签页仍然正常显示在顶部

## 如何提交
```bash
# 提交前执行的最少测试步骤
xmake build mogan
./build/mogan
```

## 2026/05/08 第一步：隐藏菜单项

### What
在 `view-menu.scm` 中移除 `("Tab bar" (toggle-visible-icon-bar 4))` 菜单项。

### Why
现在标签页固定在顶部，点击 `查看 -> 标签页工具栏` 会导致界面错乱，且实际上并不能隐藏标签页。

### How
直接注释掉或删除 `view-menu.scm` 第 148 行的菜单项定义。

## 2026/05/08 第二步：清理相关代码 [待完成]

### What
移除与标签页工具栏显示/隐藏相关的所有代码，包括：
- Scheme 层的 `tab bar` 偏好设置和 `toggle-visible-icon-bar` 中 index=4 的处理
- C++ 层 `new_window.cpp` 中对 `tab bar` 偏好的读取
- C++ 层 `tm_window.cpp` 中 `which == 4` 的 tab pages visibility 映射
- C++ 层 `qt_tm_widget.cpp` 中 `SLOT_TAB_PAGES_VISIBILITY` 的处理

### Why
标签页工具栏已是核心 UI 元素，不可隐藏，相关的切换逻辑成为死代码。

### How
待第一步合并后再执行。
