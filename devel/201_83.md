# [201_83] 为 `\emptyset` 增加 Tab Cycling 和 Lego Symbol 快捷键

## 如何测试
1. 进入数学模式
2. 输入 `@ /` 后 `tab` 两次
3. 输入 `0 tab`
4. 输入 `\emptyset`
5. 观察符号是否一致

## 2026/2/13
### What
为 `\emptyset` 增加 Tab Cycling 和 Lego Symbol 快捷键