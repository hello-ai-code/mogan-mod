# [0601] 将缩进段落从页面布局菜单移到段落菜单

## 如何测试
1. 打开 Mogan STEM，新建一个空白文档。
2. 确认当前处于默认模式（焦点在文档正文时）。
3. 点击菜单栏 **文档 -> 页面 -> 布局**，确认其中**不再包含**"缩进段落"选项。

## 2026/05/11
### What
将"缩进段落"(Indent paragraphs) 这一功能入口从 **页面 -> 布局** 菜单中移除。

### Why

因为如果文档段落设置了首行缩进，那么页面->缩进段落这个入口就不生效了