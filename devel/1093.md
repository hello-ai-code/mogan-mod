# [1093] 格式化 TeXmacs/progs/version 目录下的 .scm 文件

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [1092.md](1092.md) - 上一批次：格式化 `texmacs/progs/client` 目录

## 2 任务相关的代码文件
- `tools/format/liii/goldformat-path.scm` — `scm-dirs` 新增 `TeXmacs/progs/version` 目录
- `TeXmacs/progs/version/version-compare.scm` — 执行 `gf fmt`，统一缩进与括号风格并补齐尾注释
- `TeXmacs/progs/version/version-drd.scm` — 同上
- `TeXmacs/progs/version/version-edit.scm` — 同上
- `TeXmacs/progs/version/version-git.scm` — 同上
- `TeXmacs/progs/version/version-kbd.scm` — 同上
- `TeXmacs/progs/version/version-menu.scm` — 同上
- `TeXmacs/progs/version/version-svn.scm` — 同上
- `TeXmacs/progs/version/version-tmfs.scm` — 同上

> 注：本批次不新增 `gfexclude.json` 条目，`version/` 下所有 8 个 `.scm` 全量纳入格式化。

## 3 如何测试

### 3.1 确定性测试（单元测试）
本任务为纯格式化 + 配置改动，无对应 C++/Scheme 单元测试。

### 3.2 非确定性测试（脚本与文档验证）
```bash
# 1. 对 version 目录跑 gf fmt，确认全部 .scm 正常格式化、自动尊重 gfexclude.json
gf fmt TeXmacs/progs/version

# 2. 确认幂等（再次执行 Files updated: 0）
gf fmt TeXmacs/progs/version
git diff --stat TeXmacs/progs/version

# 3. mogan 批量脚本（gf format）也应能覆盖 version 目录、并尊重 gfexclude.json
gf format

# 4. 编译并启动 MoganSTEM，确认版本管理（versioning）相关功能无回归
xmake b stem
xmake r stem
```

手动验证（启动后逐项检查）：
- 版本差异浏览：在含版本历史的文档中，跳到第一处/上一处/下一处/最后一处差异（`version-compare.scm` / `version-edit.scm` 中 `version-first-difference` 等）正常
- 版本视图切换：并排/仅旧版/仅新版（`version-show 'version-both/old/new`）显示正确
- 版本保留选择（`version-retain` current/0/1）应用生效
- Git/SVN 版本工具集成：`version-git.scm` / `version-svn.scm` 的提交/检出/差异拉取流程无异常
- 版本菜单项（`version-menu.scm`）与版本相关快捷键（`version-kbd.scm` 中的 `version home/up/down/...`）响应正常
- tmfs 版本虚拟文件系统挂载/读写（`version-tmfs.scm`）行为不变
- DRD 规则（`version-drd.scm`）注册不受尾注释重排影响

## 4 如何提交

提交前执行以下最少步骤：

```bash
gf fmt --changed-since=main
xmake b stem
```

## 5 What
1. 把 `TeXmacs/progs/version` 加入 `goldformat-path.scm` 的 `scm-dirs`。
2. 对 `TeXmacs/progs/version` 目录下全部 8 个 `.scm` 文件执行 `gf fmt`。

## 6 Why
`TeXmacs/progs/version` 是 Mogan 版本管理（版本差异比较、Git/SVN 集成、版本 tmfs、版本菜单与快捷键）的核心 Scheme 代码目录，此前一直未纳入 `gf fmt` 的格式化路径，目录内 `.scm` 缩进/括号风格不统一、缺少尾注释，diff 噪声大、可读性差。

本批次目录内不存在像 1091 中 `prefix-kbd.scm` 那样需人工紧凑维护的大段平铺条目——`version-kbd.scm` 虽含 `kbd-map` 条目，但条目数量有限、重排后风格清晰且 diff 可控，无需在 `gfexclude.json` 中新增排除项，8 个文件全量格式化即可。

## 7 How
1. 在 `goldformat-path.scm` 的 `scm-dirs` 追加 `"TeXmacs/progs/version"`。
2. 执行 `gf fmt TeXmacs/progs/version`，对全部 8 个 `.scm` 统一缩进与括号风格、补齐尾注释。
3. 校验：对 `version` 目录再次执行 `gf fmt` 应输出 `Files updated: 0`（幂等）；编译并启动应用，确认版本差异浏览/视图切换/Git-SVN 集成/菜单快捷键/tmfs 等功能无回归。
