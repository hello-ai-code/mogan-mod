# [0372] 将 math 目录纳入格式化范围并执行格式化

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [Format_ZH.md](Format_ZH.md) - 仓库格式化相关说明

## 2 任务相关的代码文件
- `tools/format/liii/goldformat-path.scm` - 扩充 formatter 扫描路径，纳入 `TeXmacs/progs/math`
- `TeXmacs/progs/math/math-adjust-en.scm` - 仅格式化
- `TeXmacs/progs/math/math-adjust-fr.scm` - 仅格式化
- `TeXmacs/progs/math/math-drd.scm` - 仅格式化
- `TeXmacs/progs/math/math-edit.scm` - 仅格式化
- `TeXmacs/progs/math/math-markup.scm` - 仅格式化
- `TeXmacs/progs/math/math-menu.scm` - 仅格式化
- `TeXmacs/progs/math/math-sem-edit.scm` - 仅格式化
- `TeXmacs/progs/math/math-speech-en.scm` - 仅格式化
- `TeXmacs/progs/math/math-speech-fr.scm` - 仅格式化
- `TeXmacs/progs/math/math-speech.scm` - 仅格式化
- `TeXmacs/progs/math/math-stats.scm` - 仅格式化

## 3 如何测试

### 3.1 确定性测试（单元测试）
```bash
# 本任务仅涉及格式化，无新增单元测试
```

### 3.2 非确定性测试（文档验证）
```bash
# 重新运行格式化工具，确认输出稳定且不会继续产生新的 diff
```

人工确认：
1. `tools/format/liii/goldformat-path.scm` 中包含 `TeXmacs/progs/math`
2. `TeXmacs/progs/math` 下本次变更文件仅表现为排版、缩进、换行和注释位置调整
3. 重跑 formatter 后，不再继续改写这些文件

## 4 如何提交

提交前执行以下最少步骤：

```bash
git diff --check
git diff --cached --check
```

## 5 What
把 `TeXmacs/progs/math` 纳入现有 Scheme 格式化路径，并对 math 目录下当前命中的 Scheme 文件执行一次统一格式化。

1. 在 `goldformat-path.scm` 中新增 `TeXmacs/progs/math`
2. 对数学规则、数学菜单、语义编辑、语音播报与统计相关 Scheme 文件执行批量 format
3. 保持任务范围为“代码样式整理”，不引入预期功能变更

## 6 Why
此前 formatter 路径未覆盖 `TeXmacs/progs/math`，导致数学系统相关 Scheme 文件的缩进、换行、括号闭合注释风格与仓库其他目录不一致。

把 math 目录纳入统一格式化范围后，可以：

1. 降低后续功能改动时的样式噪音
2. 让 math 相关模块与仓库现有 Scheme 代码保持一致风格
3. 避免该目录继续长期处于“未被 formatter 管理”的状态

## 7 How
实现方式与前一批格式化任务一致：

1. 更新 `tools/format/liii/goldformat-path.scm` 的 Scheme 目录列表，把 `TeXmacs/progs/math` 加进去
2. 基于现有格式化工具对 math 目录下命中的 Scheme 文件批量执行 format
3. 产出 diff 时重点约束为样式变化，包括：
   - 多行表达式重新缩进
   - `define-group`、`menu-bind`、`texmacs-module` 等结构的换行与对齐统一
   - 括号位置与闭合注释整理
   - 长行拆分
   - 少量注释空格和布局统一

本任务不修改算法、控制流或模块接口；如果后续 review 发现行为差异，应按 formatter 误改另行处理，而不是视为本任务目标的一部分。
