# [222_41] 页码改进

## 相关文档
- [x_y.md](x_y.md) - 任务文档模板
- [202_9.md](202_9.md) - 新增页码样式功能 & 修复页码显示

## 任务相关的代码文件
- `TeXmacs/progs/generic/document-widgets.scm`
  - 新增 `open-document-page-number` 函数
  - 新增 `page-number-style-tool` 侧边栏工具定义
- `TeXmacs/progs/generic/document-menu.scm`
  - 在 `full-document-menu` 和 `compressed-document-menu` 中新增 `Page number` 菜单项
  - 新增 `document-page-number-menu` 菜单定义
- `TeXmacs/progs/init-research.scm`
  - 将 `open-document-page-number` 加入 `lazy-define`

## 如何测试
1. 启动 Mogan： `xmake r stem`
2. 打开 `文档 -> 页面编码`，检查是否有 `Page number` 菜单项
3. 点击 `Page number`，检查是否在右侧侧边栏打开页码样式设置面板
4. 填写起始页、结束页、第一页、页码样式，点击 Ok，检查页码是否正确应用

## 如何提交
```bash
xmake r stem
```
启动应用进行功能验证。

## 2026/05/07 页码改进

### What
将 `文档 -> 页面` 中的 `新页码样式层` 按钮迁移到新增的 `文档 -> 页面编码` 菜单项。功能交互从弹出窗口改为右侧侧边栏面板。

### Why

### How

