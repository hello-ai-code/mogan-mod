# 201_63 文本选中悬浮框

## 如何测试
- 关闭文本选中悬浮框时先输入 `xmake config -vD --text_toolbar=false`
- 打开文本选中悬浮框继续开发时先输入 `xmake config -vD --text_toolbar=true`

### 手动测试
1. **基本功能测试**
   - 选中一段文本，测试是否会弹出文本工具栏
   - 点击工具栏外部，测试是否会隐藏
   - 滚动页面，测试工具栏是否跟随移动
   - 额外检查macos上文本选中悬浮框尺寸是否合适

2. **显示条件测试**

   **会显示的情况**（需同时满足）：
   - ✅ 不在数学模式（`in-math?`）
   - ✅ 不在编程模式（`in-prog?`）
   - ✅ 不在代码模式（`in-code?`）
   - ✅ 不在原文模式（`in-verbatim?`）
   - ✅ 有活动的文本选区
   - ✅ 选区内容非空

   **不会显示的情况**（满足任一）：
   - ❌ 在数学公式中选中
   - ❌ 在代码块中选中
   - ❌ 在程序块中选中
   - ❌ 在原文环境中选中
   - ❌ 没有选区（仅光标）
   - ❌ 选区为空字符串
   - ❌ 正在拖拽鼠标（`left_dragging`）
   - ❌ 选区不在视图范围内

3. **缓存机制测试**
   - 快速移动鼠标，观察工具栏是否稳定（100ms缓存）
   - 选区变化后，观察是否及时更新

4. **按键功能测试**
   - 测试按键行为是否和图标一致

### 自动测试

#### cpp
```bash
# 运行脚本
./bin/test_only text_toolbar_test
```
#### scheme
```bash
# 运行脚本
./bin/test_only 201_63
```

**测试覆盖**:
- 缓存机制验证
- 缓存失效功能
- 空选区处理
- 有效选区矩形计算
- 坐标转换一致性

## 2026/04/02 通过 xmake 参数控制文本选中悬浮框

### What
- 添加 `text_toolbar` 编译选项
- 默认关闭文本选中悬浮框
- 通过 `USE_TEXT_TOOLBAR` 宏控制 `should_show_text_toolbar ()`

#### 修改文件：

**xmake.lua**（修改）
- 添加 `option("text_toolbar")`
- 默认值设为 `false`
- 在两个 `config.h` 生成入口中透传 `USE_TEXT_TOOLBAR = has_config("text_toolbar")`

**src/System/config.h.xmake**（修改）
- 添加 `${define USE_TEXT_TOOLBAR}` 宏

**src/Edit/Interface/edit_mouse.cpp**（修改）
- 在 `should_show_text_toolbar ()` 中使用 `#ifndef USE_TEXT_TOOLBAR`
- 关闭时直接返回 `false`
- 打开时保留原有文本选中悬浮框显示逻辑

### Why
- 当前希望默认关闭文本选中悬浮框
- 后续仍需要保留这套实现，便于继续开发和调试
- 参考 `216_1` 中 `startup_tab` 的编译开关模式，降低维护成本

### How
1. **xmake 编译参数**：
   - `xmake config -vD --text_toolbar=false` 关闭文本选中悬浮框
   - `xmake config -vD --text_toolbar=true` 打开文本选中悬浮框

2. **宏生成链路**：
   - `xmake.lua` 将 `text_toolbar` 选项写入 `USE_TEXT_TOOLBAR`
   - `src/System/config.h.xmake` 生成 `config.h` 宏

3. **运行时控制**：
   - 未定义 `USE_TEXT_TOOLBAR` 时，`should_show_text_toolbar ()` 直接返回 `false`
   - 定义 `USE_TEXT_TOOLBAR` 时，继续执行原有显示条件判断

## 2026/3/31 删除章节和语义块按钮

## 2026/3/30 针对macos特调css

## 2026/3/13 通过 scheme 加入具体的按钮
### What
- 添加功能按键
- 优化部分代码
- 补充scheme测试，优化cpp测试

## 2026/3/6 性能优化与代码清理
### What
- 基于 Chen Jie 已经实现的代码，进行拆分优化
   - 添加 should_show_text_toolbar() 缓存机制，减少 Scheme 调用
   - 消除 selection_active_any() 重复检查
   - 简化 update_text_toolbar() 中的冗余计算

### Why
- 每次鼠标移动调用 4 次 Scheme call()，性能开销大
- selection_active_any() 被重复调用
- min/max 计算冗余
- 存在未使用的代码残留

### How
1. **添加缓存字段**（edit_interface.hpp）:
   ```cpp
   time_t text_toolbar_last_check = 0;
   bool   text_toolbar_last_result = false;
   ```

2. **优化 should_show_text_toolbar()**:
   - 使用 100ms 缓存，避免频繁 Scheme 调用
   - 缓存上次检查结果和时间戳

3. **简化 get_text_selection_rect()**:
   - 单点检查 selection_active_any()
   - 移除重复逻辑分支

4. **重构 update_text_toolbar()**:
   - 早期返回无效矩形
   - 移除冗余 min/max 计算
   - 简化控制流程

5. **修复类型转换安全性**（edit_mouse.cpp）:
   - static_cast 改为 dynamic_cast
   - 添加空指针检查
   ```cpp
   // 修改前
   qt_simple_widget_rep* qsw = static_cast<qt_simple_widget_rep*>(this);
   
   // 修改后
   if (qt_simple_widget_rep* qsw = dynamic_cast<qt_simple_widget_rep*>(this)) {
       qsw->show_text_toolbar(...);
   }
   ```

6. **添加缓存失效方法**（edit_interface.hpp/cpp）:
   ```cpp
   void invalidate_text_toolbar_cache();
   // 实现：text_toolbar_last_check = 0;
   ```

7. **添加单元测试**（tests/Edit/Interface/text_toolbar_test.cpp）:
   - 测试缓存机制和失效
   - 测试矩形有效性验证
