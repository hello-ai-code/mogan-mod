# [1097] 格式化 TeXmacs/progs/various 目录下的 .scm 文件

## 1 相关文档
- [dddd.md](dddd.md) - 任务文档模板
- [1093.md](1093.md) - 上一批次：格式化 `TeXmacs/progs/version` 目录

## 2 任务相关的代码文件
- `tools/format/liii/goldformat-path.scm` — `scm-dirs` 新增 `TeXmacs/progs/various` 目录
- `TeXmacs/progs/various/poster-drd.scm` — 执行 `gf fmt`，统一缩进与括号风格并补齐尾注释
- `TeXmacs/progs/various/poster-edit.scm` — 同上
- `TeXmacs/progs/various/poster-menu.scm` — 同上
- `TeXmacs/progs/various/theme-edit.scm` — 同上
- `TeXmacs/progs/various/theme-menu.scm` — 同上

> 注：本批次不新增 `gfexclude.json` 条目，`various/` 下全部 5 个 `.scm` 全量纳入格式化。

## 3 如何测试

### 3.1 确定性测试（单元测试）
本任务为纯格式化 + 配置改动，无对应 C++/Scheme 单元测试。

### 3.2 非确定性测试（脚本与文档验证）
```bash
# 1. 对 various 目录跑 gf fmt，确认全部 .scm 正常格式化、自动尊重 gfexclude.json
gf fmt TeXmacs/progs/various

# 2. 确认幂等（再次执行 Files updated: 0）
gf fmt TeXmacs/progs/various
git diff --stat TeXmacs/progs/various

# 3. mogan 批量脚本（gf format）也应能覆盖 various 目录、并尊重 gfexclude.json
gf format

# 4. 编译并启动 MoganSTEM，确认海报（poster）与主题（theme）相关功能无回归
xmake b stem
xmake r stem
```

手动验证（启动后逐项检查）：
- 海报模式编辑：进入/退出 poster 模式，海报块（poster-block）的插入、嵌套与分栏布局正常（`poster-edit.scm` 中 `poster-insert-*` / `poster-...-insert` 等）
- 海报 DRD 规则（`poster-drd.scm`）注册不受尾注释重排影响，海报相关标签在 DRD 检查中行为不变
- 海报菜单项（`poster-menu.scm`）响应正常，各海报块/分栏的菜单入口可触发
- 主题编辑：`theme-edit.scm` 中主题颜色、字体、间距等偏好项可被读写与应用，切换主题后界面即时刷新
- 主题菜单项（`theme-menu.scm`）响应正常，主题选择/预览/应用流程无异常

## 4 如何提交

提交前执行以下最少步骤：

```bash
gf fmt --changed-since=main
xmake b stem
```

## 5 What
1. 把 `TeXmacs/progs/various` 加入 `goldformat-path.scm` 的 `scm-dirs`。
2. 对 `TeXmacs/progs/various` 目录下全部 5 个 `.scm` 文件执行 `gf fmt`。

## 6 Why
`TeXmacs/progs/various` 是 Mogan 海报（poster）与主题（theme）功能的 Scheme 代码目录，此前一直未纳入 `gf fmt` 的格式化路径，目录内 `.scm` 缩进/括号风格不统一、缺少尾注释，diff 噪声大、可读性差。

本批次目录内不存在像 1091 中 `prefix-kbd.scm` 那样需人工紧凑维护的大段平铺条目——`poster-menu.scm` / `theme-menu.scm` 虽含菜单条目，但条目数量有限、重排后风格清晰且 diff 可控，无需在 `gfexclude.json` 中新增排除项，5 个文件全量格式化即可。

## 7 How
1. 在 `goldformat-path.scm` 的 `scm-dirs` 追加 `"TeXmacs/progs/various"`。
2. 执行 `gf fmt TeXmacs/progs/various`，对全部 5 个 `.scm` 统一缩进与括号风格、补齐尾注释。
3. 校验：对 `various` 目录再次执行 `gf fmt` 应输出 `Files updated: 0`（幂等）；编译并启动应用，确认海报模式编辑/海报菜单/主题编辑/主题菜单等功能无回归。
